# Dependencies
node_modules

# Production build output
build

# Environment variable files — NEVER commit these
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# .env.example IS committed (it has no real values, just placeholders)

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# OS
.DS_Store
Thumbs.db

# Generated release metadata — produced at build time by scripts/generate-changelog.mjs.
# Never commit these; Vercel regenerates them on every deploy.
public/version.json
public/changelog.json
